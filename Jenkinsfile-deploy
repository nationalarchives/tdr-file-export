library("tdr-jenkinslib")

ecsDeployJob(
    imageName: "consignment-export",
    toDeploy: params.TO_DEPLOY,
    ecsService: "consignment_export",
    stage: params.STAGE,
    testDelaySeconds: 300
)

